package com.Selnm.tests;

public class FindElementListElement {
    /*Both findElement and findElements are WebDriver's methods.
    findElement --> brings the first element it finds
    findElements --> brings the all elements it finds
    List<WebElement> list = findElements(By.className("nav-left"))--> you can reach out to List interface and use elements
    list.get(0); -->you can reach out the 1st element
     */
}
